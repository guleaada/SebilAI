# Keep the image small AND avoid copying the host's (macOS) node_modules over
# the Linux modules installed by `npm ci` inside the container.
node_modules
npm-debug.log
.git
.gitignore
.env
.env.local
.env.*.local
*.db
*.db-shm
*.db-wal
*.db-journal
sebilai_dump.sql
.DS_Store
*.log
*.pptx
*.ipynb
